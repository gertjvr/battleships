{
  "name": "kids-battleships",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.7.0",
  "workspaces": [
    "web",
    "engine",
    "convex"
  ],
  "scripts": {
    "dev": "concurrently \"pnpm -C convex dev\" \"pnpm -C web dev\"",
    "build": "pnpm -C engine build && pnpm -C web build",
    "preview": "pnpm -C web preview"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "concurrently": "^9.2.1",
    "playwright": "^1.55.0",
    "typescript": "^5.9.2"
  }
}
